version: 2.1

orbs:
  android: circleci/android@2.0.1

jobs:
  machines:
    parameters:
      img:
        type: string
    machine:
      image: << parameters.img >>
    steps:
      - run: echo "Machine"
    

workflows:
  main:
    jobs:
      - machines:
          matrix:
            parameters:
              img:
                - android:2022.06
                - android:2022.01.1
                - android:202102-01
